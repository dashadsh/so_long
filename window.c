
#include "so_long.h"

// mlx_init() will establish a connection to the correct graphical system 
// and will return a void * which holds the location of our current MLX instance. 

// mlx_new_window() returns a pointer to the window we have just created. 
// We can give the window height, width and a title. 

// mlx_loop() initiates the window rendering.
//--------------------------------------------------------------------------


// int main(void)
// {
// 	void *mlx;
// 	// void *win;

// 	mlx = mlx_init();
// 	mlx_new_window(mlx, 1000, 500, "GAME OVER");
// 	// win = mlx_new_window(mlx, 1000, 500, "GAME OVER"); //w w w error - variable 'win' set but not used
// 	mlx_loop(mlx);
// }

//--------------------------------------------------------------------------